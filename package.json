{
  "name": "csi",
  "version": "1.0.0",
  "description": "Category Spending Insights - Expense tracker and pivot table",
  "main": "docs/bundle.js",
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --outfile=docs/bundle.js --minify --sourcemap",
    "dev": "esbuild src/index.tsx --bundle --outfile=docs/bundle.js --sourcemap --watch",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "preact": "^10.28.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  }
}
